def build(context):
    context(
        source = [
            "__init__.py",
            "defaults.py",
            "admin.py",
            "labor.py",
            "work.py",
            "log.py",
            "cache.py",
            ],
        install_path = "${PYTHONDIR}/condor",
        )

    context.recurse("examples")

